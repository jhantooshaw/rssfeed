This application is based on fetching relevent information or data of relative feed sites. Here, you can create multiple feed url(link of feeder site). There is also a view where you can see the list of feed urls as well as add, edit or delete features.

There is a redaer page(Feed list) where you can see list of feed for respective feed url.
